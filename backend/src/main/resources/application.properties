spring.application.name=loglog-backend
server.port=8080

# Token API — set via environment variables in production
token.api.url=${TOKEN_API_URL:http://localhost:9000/rewards}
token.api.key=${TOKEN_API_KEY:dev-key}
